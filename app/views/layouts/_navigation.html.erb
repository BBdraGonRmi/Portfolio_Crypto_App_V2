<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <a class="navbar-brand" href="/">
            <%= image_tag("crypto_icon.png", :alt => "logo", :size => "32x32", class:"d-inline-block align-text-bottom") %>
            PORTFOLIO CRYPTO APP
        </a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav nav-underline me-auto mb-2 mb-lg-0">
                <% if logged_in? %>
                    <li class="nav-item">
                        <%= link_to "Dashboard", dashboard_path, class:"nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Wallets", wallets_path, class:"nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Transactions", transactions_path, class:"nav-link" %>
                    </li>
                <% end %>
            </ul>

            <ul class="navbar-nav mb-2 mb-lg-0">
                <% if logged_in? %>
                    <div class="nav-item dropdown nav-underline">
                        <a class="nav-link dropdown-toggle"" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= current_user.username %>
                        </a>

                        <ul class="dropdown-menu text-small">
                            <li><%= link_to "Profil", user_path(current_user), class:"dropdown-item" %></li>
                            <li><hr class="dropdown-divider"></li>
                            <li class="dropdown-item"><%= button_to "Log Out", logout_path, method: :delete, class:"btn btn-outline-warning me-2" %></li>
                        </ul>
                    </div>
                <% else%>
                    <%= button_to "Login", login_path, method: :get, class:"btn btn-outline-light me-2" %>
                    <%= button_to "Sign Up", signup_path, method: :get, class:"btn btn-outline-warning me-2" %>
                <% end %>
            </ul>
        </div>

        
    </div>
</nav>